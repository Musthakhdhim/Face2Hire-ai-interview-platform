name: CI

on:
  push:
    branches: [ master, main, develop ]
  pull_request:
    branches: [ master, main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: SYSTEM
          MYSQL_DATABASE: aiinterview
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # ---------- Backend ----------
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Cache Maven dependencies
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('face2hire-backend/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      # 1. Run Checkstyle (fails on violations)
      - name: Run Checkstyle
        run: |
          cd face2hire-backend
          mvn checkstyle:check
        env:
          # Minimal env variables needed for checkstyle (no DB required)
          JWT_SECRET: ${{ secrets.JWT_SECRET }}

      # 2. Build and test backend (runs unit/integration tests)
      - name: Build and test backend
        run: |
          cd face2hire-backend
          mvn clean verify -e -X
        env:
          DB_URL: jdbc:mysql://localhost:3306/aiinterview?createDatabaseIfNotExist=true
          DB_USERNAME: root
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          EMAIL_USERNAME: ${{ secrets.MAIL_USERNAME }}
          EMAIL_PASSWORD: ${{ secrets.MAIL_PASSWORD }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          APP_FRONTEND_URL: http://localhost:5173
          AWS_S3_BUCKET_NAME: ${{ secrets.AWS_S3_BUCKET_NAME }}
          AWS_S3_REGION: ${{ secrets.AWS_S3_REGION }}
          AWS_S3_ACCESS_KEY_ID: ${{ secrets.AWS_S3_ACCESS_KEY_ID }}
          AWS_S3_SECRET_ACCESS_KEY: ${{ secrets.AWS_S3_SECRET_ACCESS_KEY }}
          OAUTH_GITHUB_CLIENT_ID: ${{ secrets.OAUTH_GITHUB_CLIENT_ID }}
          OAUTH_GITHUB_CLIENT_SECRET: ${{ secrets.OAUTH_GITHUB_CLIENT_SECRET }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: face2hire-backend/target/surefire-reports/

      # ---------- Frontend ----------
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Cache npm dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('Face2Hire-frontend/package-lock.json') }}
          restore-keys: ${{ runner.os }}-node-

      - name: Create .env file for frontend
        run: |
          cd Face2Hire-frontend
          echo "VITE_API_BASE_URL=http://localhost:8080/api/v1" > .env

      - name: Install frontend dependencies
        run: |
          cd Face2Hire-frontend
          npm ci

      # 3. Run ESLint (fail on any error or warning)
      - name: Run ESLint
        run: |
          cd Face2Hire-frontend
          if npm run | grep -q " lint"; then
            npm run lint
          else
            npx eslint . --ext .js,.jsx,.ts,.tsx --max-warnings=0
          fi

      - name: Run frontend tests (if script exists)
        run: |
          cd Face2Hire-frontend
          if npm run | grep -q " test"; then
            npm test -- --watchAll=false --passWithNoTests
          else
            echo "No test script found – skipping tests"
          fi

      - name: Build frontend
        run: |
          cd Face2Hire-frontend
          npm run build